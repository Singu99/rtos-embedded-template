#include "global_io.hpp"

#include <etl/unordered_map.h>

namespace pal {


    /*
    static constexpr inline etl::vector<gpio, io_count> global_io::create_gpio_table()
    {
        etl::vector<gpio, io_count> gpio_table;
        gpio_table.reserve(io_count);

        for (uint32_t port_counter = 0; port_counter < static_cast<uint32_t>(port::COUNT); ++port_counter) {
            for (uint32_t pin_counter = 0; pin_counter < static_cast<uint32_t>(pin::COUNT); ++pin_counter) {
                gpio_table.emplace_back(static_cast<port>(port_counter), static_cast<pin>(pin_counter));
            }
        }

        return gpio_table;
    }
    */







}

